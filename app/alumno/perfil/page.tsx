import { PageTitle } from '@/components/PageTitle';import { requireAuth } from '@/lib/auth/session';import { updateOwnProfileAction } from '@/features/control/profile-actions';
export default async function Profile(){const c=await requireAuth(['ALUMNO']);return <><PageTitle eyebrow="Cuenta" title="Mi perfil" description="Los datos académicos oficiales los administra Control Escolar. Aquí puedes actualizar información básica de presentación de tu cuenta."/><form action={updateOwnProfileAction} className="login-panel" style={{boxShadow:'none',maxWidth:'620px'}}><div className="field"><label>Nombre para mostrar</label><input aria-label="Nombre para mostrar" name="display_name" defaultValue={c.displayName} required/></div><div className="field"><label>Correo</label><input aria-label="Correo" value={c.email??''} readOnly aria-readonly="true"/></div><button className="btn btn-primary">Guardar</button> <a className="btn btn-ghost" href="/actualizar-contrasena">Cambiar contraseña</a></form></>}
