'use client';
import { useState } from 'react';
import { createSupabaseBrowserClient } from '@/lib/supabase/client';
export function UpdatePasswordForm(){const [msg,setMsg]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const a=String(f.get('password')??''),b=String(f.get('confirm')??'');if(a.length<10){setMsg('La contraseña debe tener al menos 10 caracteres.');return}if(a!==b){setMsg('Las contraseñas no coinciden.');return}const s=createSupabaseBrowserClient();const {error}=await s.auth.updateUser({password:a});setMsg(error?'No fue posible actualizar la contraseña.':'Contraseña actualizada. Ya puedes continuar.');if(!error)e.currentTarget.reset();}return <form onSubmit={submit}><div className="field"><label htmlFor="password">Nueva contraseña</label><input id="password" name="password" type="password" minLength={10} autoComplete="new-password" required/></div><div className="field"><label htmlFor="confirm">Confirmar</label><input id="confirm" name="confirm" type="password" minLength={10} autoComplete="new-password" required/></div><button className="btn btn-primary">Actualizar contraseña</button>{msg&&<div className="alert" role="status">{msg}</div>}</form>}
